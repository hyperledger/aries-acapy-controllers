version: '3'

services:
    # Agents
    faber-agent:
        command: ['faber --port 8020']
        build:
            context: https://github.com/hyperledger/aries-cloudagent-python.git
            dockerfile: ./docker/Dockerfile.demo
        ports:
            - 8020-8027:8020-8027
        networks:
            - aca
        environment:
            - RUNMODE=${RUNMODE}
            - DOCKERHOST=${DOCKERHOST}
            - LEDGER_URL=${LEDGER_URL}
            - GENESIS_URL=${GENESIS_URL}
        tty: true
    alice-agent:
        command: ['alice --port 8030']
        build:
            context: https://github.com/hyperledger/aries-cloudagent-python.git
            dockerfile: ./docker/Dockerfile.demo
        ports:
            - 8030-8037:8030-8037
        networks:
            - aca
        environment:
            - RUNMODE=${RUNMODE}
            - DOCKERHOST=${DOCKERHOST}
            - LEDGER_URL=${LEDGER_URL}
            - GENESIS_URL=${GENESIS_URL}
        tty: true
    acme-agent:
        command: ['acme --port 8040']
        build:
            context: https://github.com/petridishdev/aries-cloudagent-python.git
            dockerfile: ./docker/Dockerfile.demo
        ports:
            - 8040-8047:8040-8047
        networks:
            - aca
        environment:
            - RUNMODE=${RUNMODE}
            - DOCKERHOST=${DOCKERHOST}
            - LEDGER_URL=${LEDGER_URL}
            - GENESIS_URL=${GENESIS_URL}
        tty: true

    # Controllers
    faber-controller:
        build:
            context: ../demo/controllers/faber-controller/
        ports:
            - 9021:80
        networks:
            - aca
        depends_on:
            - faber-agent
        environment:
            - FABER_AGENT_HOST=${FABER_AGENT_HOST}
    alice-controller:
        build:
            context: ../demo/controllers/alice-controller/
        ports:
            - 9031:80
        networks:
            - aca
        depends_on:
            - alice-agent
        environment:
            - ALICE_AGENT_HOST=${ALICE_AGENT_HOST}
    acme-controller:
        build:
            context: ../demo/controllers/acme-controller/
        ports:
            - 9041:3000
        networks:
            - aca
        depends_on:
            - acme-agent
        environment:
            - ACME_AGENT_HOST=${ACME_AGENT_HOST}

networks:
    aca:
